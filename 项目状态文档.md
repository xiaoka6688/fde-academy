# 项目状态文档（PROJECT STATUS）

> 用途：任何新窗口 / 新工具 / 新会话读取本文档即可接续开发，无需重新探索项目。
> 最后更新：2026-08-28

## 1. 项目一句话

**FDE 学习中心（fde-academy）**——基于 Docusaurus 3 的 AI 前沿部署工程师（FDE）系统性学习平台，已部署到 Cloudflare Pages，push 即自动部署。

## 2. 关键信息速查

| 项 | 值 |
|---|---|
| 本地路径 | `f:\AICoding\fde-learning-master` |
| GitHub 仓库 | https://github.com/xiaoka6688/fde-academy（公开，master 分支） |
| 部署平台 | Cloudflare Pages（Git 集成，自动构建） |
| 线上地址 | **https://fde.pojuai.com**（自定义域名，腾讯云 DNSPod CNAME 接入） |
| 备用地址 | https://fde-academy-ba6.pages.dev（CF 默认域名，同样可访问） |
| DNS 记录 | 腾讯云 pojuai.com：`fde` CNAME `fde-academy-ba6.pages.dev`（TTL 600） |
| 本地启动 | 双击 `FDE一键启动.exe`（自动选端口/装依赖/开浏览器）；或 `npm start` |
| Node 版本 | 本地 v22.16.0；云端环境变量 `NODE_VERSION=22` |
| 构建命令 | `npm run build`，输出目录 `build/` |
| git 身份 | xiaoka6688（凭据已存于本机，推送免交互） |

## 3. 已完成里程碑（按时间序）

1. ✅ 项目结构分析 + 新手指南（`新手使用指南.md`，9 章）
2. ✅ 一键启动器 `FDE一键启动.exe`（源码 `scripts/start-fde.cs`，重编译脚本 `scripts/rebuild-launcher.bat`）
3. ✅ Logo 品牌替换（`logo.png` → 导航栏/浏览器图标/exe 图标；生成脚本 `scripts/generate-logo-assets.ps1`）
4. ✅ 清理历史构建残留 ~14MB（根目录编号文件夹、index.html、assets/、img/ 等旧构建产物）
5. ✅ Git 初始化并推送 GitHub（仓库 fde-academy，含首次提交 + 后续提交）
6. ✅ Cloudflare Pages 部署接入（GitHub App 授权 → 仓库连接 → 构建配置 → 首次部署）
   - 构建配置：`npm run build` / 输出 `build` / `NODE_VERSION=22` / 生产分支 master
   - GitHub App 权限：仅 fde-academy 一个仓库（最小权限）

## 4. 架构速览（详细版见《新手使用指南.md》第 2 章）

```
f:\AICoding\fde-learning-master\
├── docusaurus.config.js      # 核心配置（url 指向 pages.dev，baseUrl=/）
├── docs/                     # 主文档线（99 篇，L1~L5 学习路径）
├── docs-agentic-ai/          # Agentic AI 线（20 篇，routeBasePath=/）
├── docs-opensource/          # 开源项目解读（39 篇）
├── docs-tools/               # 工具教程（6 篇）
├── blog/                     # 博客（9 篇）
├── src/pages/                # React 动态页面（读 static/data/*.json）
├── src/components/           # 组件（含 DocCard 双栏卡片）
├── sidebars/                 # 4 个侧边栏配置
├── scripts/                  # 启动器源码/重编译/图标生成脚本
├── static/img/               # logo.png + favicon.ico（由 generate-logo-assets.ps1 生成）
├── FDE一键启动.exe           # 本地一键启动器
├── 新手使用指南.md            # 新人上手文档
├── 项目经验文档.md            # 经验教训记录（踩坑手册）
└── 项目状态文档.md            # 本文档（接续开发的入口）
```

## 5. 当前状态（截至 2026-08-28）

- [x] Cloudflare Pages 项目已创建，首次部署成功（2026-08-20 20:18）
- [x] **自定义域名已绑定**：fde.pojuai.com 状态"活动"、SSL 已启用，https 访问验证通过
- [x] DNS：腾讯云 DNSPod 已添加 `fde` CNAME → `fde-academy-ba6.pages.dev`
- [x] `docusaurus.config.js` 的 url 已改为 `https://fde.pojuai.com` 并推送
- [x] **招聘动态自动更新链路已建立**（2026-08-28）
  - ZCode 定时自动化 `automation-96a1351a`：每周一 9:30 联网采集岗位 → 更新 `static/data/jobs.json`（新增岗位带 `first_seen` 字段）→ 存快照 `static/data/history/` → git push 自动部署
  - `jobs.tsx` 页面新增：近两周新增岗位统计、数据新鲜度警示（>30 天未更新自动提示）、热门技能按 tags 出现次数自动统计
  - 注意：定时任务触发时需本机开机且 ZCode 客户端运行；若停更超过 30 天页面会显示警示
- [x] **趋势数据补更新**（2026-08-29）：行业趋势 30→49 条（覆盖至 2026.08）、GitHub 趋势 +4 项目、时间范围文案改为动态生成（trends.tsx 读 data_period，不再写死）
- [ ] **AI 趋势定时采集待创建**：因"定时任务会话内不能创建新定时任务"系统限制，需在普通会话按 `.claude/skills/trend-collector.md` 第 0 节的提示词创建（每周三 9:30）
- [ ] 日常开发：push master 即自动部署到 fde.pojuai.com

## 6. 待办 / 后续优化方向

- 内容扩充：四条文档线持续新增文章
- 可考虑：搜索增强（Algolia/Docusaurus 本地搜索）、i18n、访问统计（CF Web Analytics）
- 后期如需 API 化操作 Cloudflare（CI 清缓存等），可创建 API Token（权限仅限 Pages:Edit），用 wrangler CLI 或 REST API 调用，详见《项目经验文档.md》第 8 节

## 7. 重要配置变更记录（防止回退）

| 文件 | 变更 | 原因 |
|---|---|---|
| `docusaurus.config.js` | `url: 'https://fde.pojuai.com'`，`baseUrl: '/'` | 从 GitHub Pages 迁移到 Cloudflare Pages |
| `docusaurus.config.js` | 删除 4 处 `editUrl`（原指向 luoboask 仓库） | fork 来源清理 |
| `docusaurus.config.js` | 导航 GitHub 链接 → xiaoka6688/fde-academy | 指向自己的仓库 |
| `.github/workflows/deploy.yml` | 已删除 | 不再用 GitHub Pages 部署，避免 push 报错 |
| `scripts/start-fde.cs` | `SitePath = "/"` | baseUrl 变更同步，exe 已重新编译 |
| `.gitignore` | 增加 `.vercel/`、`.wrangler/` | 部署 CLI 目录排除 |

## 8. 常用命令

```bash
npm start                    # 本地开发（默认 3000，被占用时启动器自动换 3001）
npm run build                # 生产构建（Cloudflare Pages 执行的同一命令）
git add -A && git commit -m "..." && git push origin master   # 推送即自动部署
```

## 9. 接续开发时先读

1. 本文档（当前状态）
2. `项目经验文档.md`（踩坑记录，避免重复踩坑）
3. `新手使用指南.md`（架构与操作细节）
