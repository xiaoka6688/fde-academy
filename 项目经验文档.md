# 项目经验文档（LESSONS LEARNED）

> 记录 fde-academy 项目从 0 到部署上线全过程中踩过的坑和沉淀的经验，避免重复踩坑。
> 最后更新：2026-08-20

## 1. Docusaurus 项目经验

### 1.1 一套 Docusaurus 跑多套文档
- 一个 `docusaurus.config.js` 里配置多个 `plugins: [["@docusaurus/plugin-content-docs", {...}]]` 实例（id 不同 + 各自 `routeBasePath`），即可实现 docs/、docs-agentic-ai/、docs-opensource/、docs-tools/ 四条文档线共存
- 主 docs 线 `routeBasePath: '/'`（占据根路径），其他线各有子路径
- 侧边栏配置在 `sidebars/` 目录按文档线拆分文件

### 1.2 构建前必须先 git commit
- Docusaurus 配置了 `showLastUpdateTime`（读 git 历史生成"最后更新"时间），**未提交的仓库直接 `npm run build` 会失败**
- 报错特征：构建过程报 git 相关错误。解法：先完成首次 commit 再构建

### 1.3 历史构建产物清理
- 根目录出现 `01-ai-basics/` 等编号文件夹、`index.html`、`assets/`、`img/`、`sitemap.xml`——都是**早期某次构建直接输出到根目录的残留**
- 判别方法：与 `build/` 目录内容结构一致、且当前部署流程不引用
- 清理 ~14MB（230 个文件），仓库体积减半；`.gitignore` 已含 `build/` 防止再次混入
- 教训：**构建输出目录不要随手改成根目录**

### 1.4 迁移部署平台时的配置联动
从 GitHub Pages 迁到 Cloudflare Pages 需要改一串地方，漏一处就有隐性 bug：
| 位置 | GitHub Pages | Cloudflare Pages |
|---|---|---|
| `url` | github.io 域名 | xxx.pages.dev |
| `baseUrl` | `/仓库名/` | `/` |
| navbar GitHub 链接 | 原仓库 | 新仓库 |
| 4 处 `editUrl` | 指向原仓库（fork 项目要删） | 删除或改新仓库 |
| deploy 工作流 | `.github/workflows/deploy.yml` | **删除**（否则每次 push 都报错） |
| 本地启动器 SitePath | `/仓库名/` | `/`（要重新编译 exe） |
| README/指南里的 URL | 全局替换 | 全局替换 |

## 2. Windows 环境经验

### 2.1 端口冲突
- 本机 3000 端口 IPv4 侧被 MuseBox 软件占用，Docusaurus 绑定 IPv6 侧
- Chrome 能访问，但为稳妥启动器做了**智能端口选择**：3000 被占自动换 3001
- 排查命令：`Get-NetTCPConnection -State Listen -LocalPort 3000`

### 2.2 bat 脚本编码
- 中文 Windows 的 cmd 用 **GBK** 解析 bat：必须以 GBK 编码 + CRLF 换行保存，否则中文注释导致解析失败
- 兼容方案：脚本开头 `chcp 936`

### 2.3 无需安装的 C# 编译器
- Windows 自带 .NET Framework 的 `csc.exe`（路径 `$env:WINDIR\Microsoft.NET\Framework64\v4.0.30319\csc.exe`），可直接把 `.cs` 编译成 exe
- `/win32icon:` 参数可给 exe 嵌入图标
- 适合做轻量启动器：检测环境→装依赖→选端口→起服务→轮询就绪→开浏览器

### 2.4 PowerShell 不支持 heredoc
- `git commit -m "$(cat <<'EOF' ...)"` 在 PowerShell 报语法错误
- 替代：多个 `-m` 参数拼接提交说明

## 3. 图标资源生成

- Docusaurus 导航 logo：`static/img/logo.png`（128px 足够）
- 浏览器标签 favicon：需要多尺寸 `.ico`（16/32/48/64/128/256），用 `System.Drawing` 逐级缩放生成
- **自包含深色圆角图标**（如本项目 logo）在深浅两种主题下都可读，无需做两套
- 脚本 `scripts/generate-logo-assets.ps1`：换 logo 时替换根目录 `logo.png` 重跑即可，同时重新编译 exe 嵌入图标

## 4. Cloudflare Pages 部署经验

### 4.1 平台选型结论
| 平台 | 结论 |
|---|---|
| Cloudflare Pages ✅ | 带宽无限、500 次构建/月、分支预览、允许商用，国内相对可用 |
| Vercel | 体验最好但 Hobby 禁商用、国内基本被墙 |
| GitHub Pages | 零配置但无预览链接、github.io 被墙 |

### 4.2 接入流程要点
1. dash.cloudflare.com → Workers & Pages → Create → Pages → **Connect to Git**
2. GitHub App 授权时选 **Only select repositories → 只勾 fde-academy**（最小权限原则）
3. 授权会触发 GitHub **sudo mode 验证**（邮箱验证码或密码）——这步必须人工介入，AI 无法代办
4. 构建配置：Framework preset 可以不选（none），直接手填 `npm run build` + `build` + 环境变量 `NODE_VERSION=22` 即可，效果与选 Docusaurus 预设相同
5. 生产分支 master；push master 即生产部署，push 其他分支自动生成 `<分支名>.项目名.pages.dev` 预览链接

### 4.3 自定义域名（待办经验预告）
- CF Pages → Custom domains → CNAME 指向 `项目名.pages.dev`
- 腾讯云 DNS 控制台加 CNAME 记录
- 绑定后记得把 `docusaurus.config.js` 的 `url` 改成自定义域名（影响 canonical/sitemap）

## 5. 浏览器自动化经验（TraeWork Browser Extension）

### 5.1 扩展连接排查（重要！）
扩展连不上（os error 10061）时的排查链：
1. `browser_connect_plugin` / `browser_setup_plugin` 初始化
2. 检查扩展是否安装：`%LOCALAPPDATA%\Google\Chrome\User Data\Default\Extensions\` 下找 TraeWork（ID `nbmnklcgnddbjafigmlhfjhefcfcgggn`）
3. 检查原生主机注册：注册表 `HKCU:\Software\Google\Chrome\NativeMessagingHosts\com.solo.browser_bridge`
4. **检查 `browser-bridge.exe` 进程是否在跑**（监听 `127.0.0.1:19234`）
5. 不在跑的根因：扩展 service worker 休眠/未启动
6. **唤醒方法**：`Start-Process chrome.exe "chrome-extension://nbmnklcgnddbjafigmlhfjhefcfcggcn/popup/popup.html"`（注意 ID 别打错）——扩展 SW 起来后会 connectNative 拉起 browser-bridge.exe

### 5.2 快照引用（ref）规则
- 快照输出的引用是 `[eN]` 格式，**点击时传 `"eN"`（带 e 前缀）才有效**，传纯数字会报 "ref not found"
- 引用生命周期：任何 DOM 变化后失效，**点击前必须重新快照**
- `selector: "main"` 的局部快照与全量快照的引用编号不同，混用会失效
- 每轮"快照→解析→点击"放在同一个 Exec 块内最稳

### 5.3 Cloudflare 控制台页面自动化
- 页面是 React（react-select 组件），**直接 type 进文本框比操作下拉框可靠得多**
- react-select 下拉用 JS dispatch mousedown 经常打不开，别浪费时间，直接填值
- 主内容区用 `selector: "main"` 快照可避开巨大的侧边栏干扰
- 页面 URL 不变但内容会切换（SPA），**判断状态要看 DOM 内容而不是 URL**

## 6. Git / GitHub 经验

- 本机已存 GitHub 凭据（xiaoka6688），`git credential fill` 可取 token，用 GitHub API `POST /user/repos` 直接建仓库，全程免交互
- 公开仓库的 API 校验**无需认证**（`api.github.com/repos/...` 直接 GET）
- fork 来的项目第一件事：清理原作者痕迹（editUrl、navbar 链接、README 截图外链、旧 deploy workflow）

## 7. 工作方法沉淀

- **决策先行**：涉及不可逆操作（删文件、选平台、建公开仓库）先用选项卡让用户确认，再动手
- **每步验证**：配置改完必须 `npm run build` 实测；启动器改完必须冒烟测试
- **文档同步**：改了部署方式，README、新手指南、状态文档三处都要同步，否则文档互相矛盾
- **状态外化**：把项目状态写进仓库内的 md 文档（而非只存在对话里），新窗口/新工具读文件即可接续
